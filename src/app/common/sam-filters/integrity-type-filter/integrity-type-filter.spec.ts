import { IntegrityTypeFilter } from './integrity-type-filter';

describe('IntegrityTypeFilter', () => {
  it('should create an instance', () => {
    expect(new IntegrityTypeFilter()).toBeTruthy();
  });
});
